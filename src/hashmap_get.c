/*
** EPITECH PROJECT, 2021
** hashmap
** File description:
** hashmap_get
*/

#include "../includes/hashmap.h"

void *hashmap_get(hashmap_t *hashmap, unsigned long key)
{
    void *value = NULL;

    if (hashmap == NULL)
        return (NULL);
    return (value);
}